# Muul AI integration (Composer drafting + Coach chat).
# Without this key the UI still runs; the AI calls return a friendly
# "not connected yet" message instead of a draft/reply.
ANTHROPIC_API_KEY=

# Optional: override the model used for drafting/coaching.
# ANTHROPIC_MODEL=claude-sonnet-4-5
